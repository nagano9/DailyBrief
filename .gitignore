# dependencies
/node_modules

# OS
.DS_Store

# env files
.env*
!.env.example

# trending pipeline output (each fork generates its own)
/daily_reports/
/logs/

# Claude Code local state (skills + commands ARE committed; cache/state aren't)
/.claude/state/
/.claude/cache/

# typescript
*.tsbuildinfo

# screenshot originals (only the cropped versions are committed)
/docs/screenshots/originals/

# brief engine: raw source excerpts (never published) + generated site
/.cache/
/site/

# Reader profile. Personal, and a fork must not inherit someone else's
# positioning as its default — copy profile.example.json and edit.
/profile.json
